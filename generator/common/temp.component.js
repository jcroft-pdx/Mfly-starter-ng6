import template from './<%= name %>.html';
import controller from './<%= name %>.controller';
import './<%= name %>.scss';

export default {
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

import angular from 'angular';
import errorHandlerComponent from './errorHandler.component';

export default angular.module('errorHandler', [
])

.component('errorHandler', errorHandlerComponent)
.name;
